from sys import argv
import sys
import os
import re
from string import split
from string import strip
from timeit import Timer
from math import sqrt

c = sys.stdin.read(1)   #　从键盘读取字符串
import random
random.random()  # 得到随机数

import copy
copy.copy()     #浅复制
copy.deepcopy() #深复制

from inspect import isgeneratorfunction
isgeneratorfunction(fab)

import itertools  # 包容很多特殊的迭代方法
itertools.permutations([list])  # 返回一个迭代器


import types
type('abc') == types.StringType
True
type(u'abc') == types.UnicodeType
True
type([]) == types.ListType
True
type(str) == types.TypeType
True   # 所有类型本身都是TypeType




